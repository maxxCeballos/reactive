# Client

run

instalar angular
```npm install -g @angular/cli```

moverse a la carpeta cliente
```cd reactive/client``

instalar las dependencias
```npm install```

Ejecutar el cliente
```ng serve -o```